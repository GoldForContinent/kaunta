{
  "name": "kaunta-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Kaunta — multi-tenant bar ledger backend (Cloudflare Workers + D1)",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply kaunta --remote"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
